services:
  thee_app:
    build: .
    container_name: thee_app
    volumes:
      - .:/app
    working_dir: /app
    command: python thee.py

  uploader_app:
    build: .
    container_name: uploader_app
    volumes:
      - .:/app
    working_dir: /app
    command: python upload1.py
